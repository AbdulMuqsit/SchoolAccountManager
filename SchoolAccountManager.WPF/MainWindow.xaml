<Window x:Class="SchoolAccountManager.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="God's Wisdom Account Manager"
        Height="400"
        Width="700">
    <Grid DataContext="{Binding MainViewModel, Source={StaticResource Locator}}">
        <ContentControl Content="{Binding CurrentChildViewModel}" />
    </Grid>
</Window>